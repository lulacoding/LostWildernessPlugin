# One database for all tables (player, clans, progression, reputation, verification, etc.)
datasources:
  player:
    jdbc-url: "jdbc:mysql://localhost:3306/lw_player?useSSL=false&allowPublicKeyRetrieval=true"
    username: "lw"
    password: "test"
    maximum-pool-size: 20
